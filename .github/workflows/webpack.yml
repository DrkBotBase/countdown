name: Mantener Render Despierto

on:
  schedule:
    - cron: '*/13 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hacer ping a la app de Countdown
        run: curl -s https://countdown-aafp.onrender.com/ping
